<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <ImageView
            android:id="@+id/head"
            android:layout_alignParentTop="true"
            android:layout_alignParentLeft="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/kula"/>
    <ImageView
        android:id="@+id/tail"
        android:layout_toRightOf="@id/head"
        android:layout_alignTop="@id/head"
         android:layout_width="48dp"
            android:layout_height="48dp"
        android:src="@drawable/youchuan"/>

    <TextView
        android:id="@+id/changeable"
        android:layout_toRightOf="@id/tail"
        android:layout_alignTop="@id/tail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/right"
        android:paddingTop="8dp"
        android:paddingBottom="18dp"
        android:textColor="#000"
        android:paddingLeft="10dp"
        android:lines="3"
        android:text="I am on the right, Can I get 3 lines I want to see two lines in this test view. Help me!!!!!!!!!!!"/>
    
    <WebView
    	android:id="@+id/webview"
    	android:layout_width="wrap_content"
    	android:layout_height="fill_parent"
    	android:layout_below="@id/head"/>

</RelativeLayout>
